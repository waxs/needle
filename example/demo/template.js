"use strict";var container,active,table,row;var keys=function keys(data){var keys=Object.keys(data);return{keys:keys,length:keys.length}};var createTable=function createTable(parent){var el=parent?document.querySelector(parent):document.body;table=document.createElement('table');el.appendChild(table)};var createHead=function createHead(data){var head=document.createElement('thead');table.append(head);var row=document.createElement('tr');head.append(row);data.forEach(function(content){var cell=document.createElement('th');cell.textContent=content;row.append(cell)})};var createRow=function createRow(data){var row=document.createElement('tr');table.append(row);Object.values(data).forEach(function(content){var cell=document.createElement('td');cell.textContent=content;row.append(cell)})};var init=function init(data,append){row=!row&&keys(data);if(!active||append!==container){createTable(append);createHead(row.keys)}
    container=append;active=!0};var html=function html(data,append){init(data,append);createRow(data)}