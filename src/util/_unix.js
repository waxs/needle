/** ----------------------------------------
     Unix
 ---------------------------------------- */

const unix = date => Math.round(new Date().getTime() / 1000);

/** ----------------------------------------
    Exports
 ---------------------------------------- */

export default unix;